#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

int t, n, q, a[2], s[2];
ll an, ad;
string ans;

int main()
{
    cin >> t;
    for(int _ = 1; _<=t; _++)
    {
        cin >> n >> q;
        for(int i = 0; i<q; i++)
            cin >> a[i] >> s[i];
        for(int i = 0; i<q; i++)
        {
            
        }
    }
}